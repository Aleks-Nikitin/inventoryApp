<!DOCTYPE html>
<html lang="en">
<%-include("partials/head.ejs") %>
<body>
     <%-include("partials/nav.ejs") %>
      <%-include("partials/errors.ejs") %>
      <h1><%=title %></h1>

      <form <% if(locals.genre){ %>
            action="/genre/add?id=<%=genre.id %>"
        <% } %>
         action="/genre/add"
         method="post">
        <div class="formBlo">
            <label for="genname">Genre name:</label>
            <input type="text" id="genname" name="genname" <% if(locals.genre){ %>
                value="<%=genre.genname %>"
                <% } %>
            >
        </div>

        <button type="submit" class="catBtn">Submit</button>
      </form>
</body>
</html>